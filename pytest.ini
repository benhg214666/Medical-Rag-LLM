[pytest]
# 註冊自訂 marker，避免 pytest 對未知 marker 發出 PytestUnknownMarkWarning。
markers =
    integration: 需要真實 embedding 模型的整合測試（首次執行可能下載模型）。

# 測試檔一律放在 tests/，明確指定可避免 pytest 掃描 data/ 或 vector_db/。
testpaths = tests
