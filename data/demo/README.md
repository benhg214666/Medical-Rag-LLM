# Phase 8 demo data

Every record in this directory is fictional, synthetic, and de-identified. The
records exist only to demonstrate the local RAG pipeline. They contain no real
patient information and are **not for clinical use**.

The files and their ordering are intentionally stable. Do not add timestamps or
randomly generated values: document and chunk IDs are derived from file content.
