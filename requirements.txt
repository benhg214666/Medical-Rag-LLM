# Phase 1 + Phase 2 + Phase 3 依賴。
# 不指定 CUDA/ROCm wheel；Lab 的 ROCm PyTorch 由部署環境依 AMD 官方方式安裝。

# --- Web 框架（Phase 1）---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
PyYAML>=6.0

# --- 文件解析（Phase 2）---
pypdf>=4.2
python-docx>=1.1
python-multipart>=0.0.9

# --- Embedding 與 Vector Store（Phase 3；預設 CPU）---
sentence-transformers>=3.0
chromadb>=0.5

# --- 測試 ---
pytest>=8.0
httpx>=0.27
