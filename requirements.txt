# Phase 1 僅包含目前實際使用到的套件。
# 後續 RAG 相關依賴（embedding、vector store、LLM 等）會在對應 Phase 才加入。

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
PyYAML>=6.0

# 測試用
pytest>=8.0
httpx>=0.27
