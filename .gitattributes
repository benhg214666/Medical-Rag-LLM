# 統一換行符（line ending）處理。
#
# 為什麼需要這個檔案：
# Windows 預設換行符是 CRLF（\r\n），Linux / macOS 是 LF（\n）。
# 若不統一，同一份檔案在不同機器上 checkout 後 git 會誤判成「整個檔案都被修改」，
# 讓 code review 時真正的改動淹沒在大量假 diff 中。
#
# 這裡的策略：repository 內一律以 LF 儲存，checkout 到工作區時也一律為 LF。
# 這會覆蓋各機器上 core.autocrlf 的設定，確保所有人看到的結果一致。
# VSCode 能正常處理 LF，不需要額外設定。

# 預設：所有 git 判定為文字的檔案，統一使用 LF
* text=auto eol=lf

# 明確指定常見文字檔（避免自動偵測誤判）
*.py     text eol=lf
*.md     text eol=lf
*.txt    text eol=lf
*.yaml   text eol=lf
*.yml    text eol=lf
*.json   text eol=lf
*.toml   text eol=lf
*.cfg    text eol=lf
*.ini    text eol=lf
*.html   text eol=lf
*.css    text eol=lf
*.js     text eol=lf
*.ts     text eol=lf
.env.example text eol=lf
.gitignore   text eol=lf
.gitattributes text eol=lf

# Windows 專用腳本必須保留 CRLF，否則部分環境無法正確執行
*.ps1    text eol=crlf
*.bat    text eol=crlf
*.cmd    text eol=crlf

# 二進位檔案：完全不做任何轉換，否則內容會損毀
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.pdf    binary
*.docx   binary
*.xlsx   binary
*.pptx   binary
*.zip    binary
*.gz     binary
*.7z     binary
*.pkl    binary
*.bin    binary
*.pt     binary
*.pth    binary
*.onnx   binary
*.safetensors binary
*.gguf   binary
*.sqlite binary
*.db     binary
